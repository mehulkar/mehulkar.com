<div class="posts">
  <% @posts.each do |post| %>
    <%= link_to(post, class: 'post-title') do %>
      <div class="title"><%= post.title %></div>
      <span class="date"><%= post.created_at.strftime(format='%B %e, %Y') %></span>
    <% end %>
  <% end %>
</div>