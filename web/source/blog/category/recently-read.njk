---
title: Books
tags:
  - books
  - "book reviews"
---

{% set books = collections['books'] | reverseChrono %}

<section style="margin-bottom: 3rem;">
  <ul style="
    list-style-type: none;
    padding:0;
    margin-left: 0;
    display:grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    grid-row-gap: 2rem;
    grid-column-gap: 1rem;"
  >
    {% for book in books %}
      <a href="{{ book.url }}" style="text-decoration:none; color:inherit;">
        <li style="padding:0;">
          <figure>
            <img style="display:block;" src="{{book.data.image}}" alt="Book cover for '{{book.data.title}}'" title="Book cover for '{{book.data.title}}'">
            <figcaption style="font-size:0.9rem;">
              <div>{{ book.data.date | fullDate }}</div>
            </figcaption>
          </figure>
        </li>
      </a>
    {% endfor %}
  </ul>
</section>
